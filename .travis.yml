language: ruby
rvm:
  - jruby-19mode
gemfile:
  - Gemfile
services:
  - redis-server
  - rabbitmq
env:
  - DB=postgresql
before_install:
  - cd www
  - mkdir tmp
  - mkdir tmp/cache
script:
  - RAILS_ENV=test bundle exec rake --trace db:migrate:reset
  - bundle exec rspec -f d -s 57137
before_script:
  - cp config/database.travis.yml config/database.yml
  - psql -c 'create database gamerist_test' -U postgres
